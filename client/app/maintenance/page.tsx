import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Maintenance | OthrHalff',
  robots: {
    index: false,
    follow: false,
  },
};

export default function MaintenancePage() {
  return (
    <main className="min-h-screen bg-black text-white flex items-center justify-center font-mono text-sm select-none">
      invalid
    </main>
  );
}
